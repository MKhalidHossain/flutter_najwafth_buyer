---
name: Project Overview
description: Flutter buyer app (najwafth_buyer) paired with Node/Express backend — book store flow
type: project
---

Flutter buyer app at `/Users/saa/dev/flutter/najwafth_all/flutter_najwafth_buyer`.
Backend at `/Users/saa/dev/flutter/najwafth_all/backendnajwafth`.
Flutter binary: `/Users/saa/src/flutter/bin/flutter`.

**Why:** Book store buyer-side app. Backend uses MongoDB + Express. Frontend uses Flutter + Riverpod.

**How to apply:** When adding features, follow the existing layer structure: `domain/` → `data/` → `application/` → `presentation/`.
